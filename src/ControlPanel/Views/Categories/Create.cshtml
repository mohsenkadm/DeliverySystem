@{ViewData["Title"] = "إضافة تصنيف";}
<div class="page-header" data-aos="fade-down">
    <h1><i class="fas fa-tag"></i> إضافة تصنيف جديد</h1>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm"><i class="fas fa-arrow-right me-1"></i>العودة</a>
</div>
<div class="row justify-content-center">
    <div class="col-lg-5">
        <div class="card" data-aos="fade-up">
            <div class="card-header"><i class="fas fa-tag"></i> بيانات التصنيف</div>
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div class="mb-3">
                        <label class="form-label">اسم التصنيف <span class="text-danger">*</span></label>
                        <input name="Name" class="form-control" placeholder="اسم التصنيف" required />
                    </div>
                    <button type="submit" class="btn btn-primary"><i class="fas fa-save me-2"></i>حفظ</button>
                    <a asp-action="Index" class="btn btn-outline-secondary ms-2">إلغاء</a>
                </form>
            </div>
        </div>
    </div>
</div>
